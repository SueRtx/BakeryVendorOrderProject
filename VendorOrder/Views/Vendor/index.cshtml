@{
  Layout = "_Layout";
}

  <body>
    <div class="container">
      <div class="page">
        @using VendorOrder.Models;

        <h1><strong>S U N N Y * B A K E R Y</strong></h1>

        <h4 id="homePageLink"><a href='/'>Back Home</a></h4>
          <h4 id="addVendorLink"><a href='/vendor/new'>Add Vendor</a></h4>

        <h3><strong>Vendors List</strong></h3>

        @if (@Model.Count == 0)
        {
          <h4>No Vendors have been added yet!</h4>
        } 

        @foreach (Vendor vendor in Model)
        {
          <h3><a href='/vendor/@vendor.Id'>@vendor.Name</a></h3>
        }
      </div>
    </div>
  </body>
