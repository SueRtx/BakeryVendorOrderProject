@{
  Layout = "_Layout";
}

<div class="container">
  @using VendorOrder.Models;

  <h1 class="title"><strong>S U N N Y * B A K E R Y</strong></h1>
  <h2><strong>Vendors List</strong></h2>
  <h5>* Click on Vendor's name to add order *</h5>
  
  <div class="infoBox"> 
    @if (@Model.Count == 0)
    {
      <h4><strong>Vendors List is EMPTY!</strong></h4>
    } 

    @foreach (Vendor vendor in Model)
    {
      <h3 id="vendorNameList"><a href='/vendor/@vendor.Id'>@vendor.Name</a></h3>
    }
  </div>

  <h4 id="rightSideBtn"><a href='/'>Back Home</a></h4>
  <h4 id="addVendorLink"><a href='/vendor/new'>Add Vendor</a></h4>
</div>

